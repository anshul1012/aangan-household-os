# Copy to .env and fill in values. .env is gitignored — never commit real values.

# Discord
bot_token=

# Postgres (used by both the db container and the bot)
POSTGRES_USER=aangan
POSTGRES_PASSWORD=
POSTGRES_DB=aangan

# LLM / transcription (added when the parser is wired up)
# anthropic_api_key=
